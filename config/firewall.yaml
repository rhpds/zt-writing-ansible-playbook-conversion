---
# By default egress traffic is not allowed, define below the ports allowed.
egress:
  - ports:
      - protocol: TCP
        port: 443
      - protcol: TCP
        port: 8443
ingress:
  - ports:
      - protocol: TCP
        port: 8443
